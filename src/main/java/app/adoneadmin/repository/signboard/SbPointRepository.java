package app.adoneadmin.repository.signboard;

import app.adoneadmin.domain.signboard.unit.SbPoint;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SbPointRepository extends JpaRepository<SbPoint, Long> {
}
